title = Sandbox with a remote
cards = checkout commit-auto pull fetch push

[description]

여기 리모트가 있는 샌드박스가 있습니다! 가져오기, 패치(fetch), 푸시를 시도해보세요!

태그와 브랜치를 리모트로 어떻게 푸시할 수 있나요? 다시 삭제하는 방법은?

[setup yours]

echo "Line 1" > essay
git add .
git commit -m "Initial commit"

git push -u friend main

[setup friend]

git checkout main
echo "Line 2" >> essay
git commit -am "Another line"