[description]

직접 객체를 가리키는 대신, refs는 다른 refs를 가리킬 수도 있습니다!

그럴 때에는 "symbolic refs"라고 불립니다. symbolic ref를 생성하거나 업데이트하려면 다음을 사용합니다.

    git symbolic-ref <name> <ref>

"refs/rainbow"라는 symbolic ref를 생성하세요!

[setup]

# Set up environment
# 환경 설정

[setup goal]

BLOB=$(git hash-object -w --stdin)
git update-ref refs/double "$BLOB"
git symbolic-ref refs/rainbow refs/double

[win]

git symbolic-ref refs/rainbow