title = Make a commit \o/
cards = add reset checkout commit

[description]

연습을 위해, 모든 파일에 "x"가 포함된 커밋을 만드세요!

[setup]

echo a > a
echo x > b
echo x > c
git add .
git commit -m "Initial commit"
echo x > a
echo b > b
git add b
echo c > c

[win]

# 파일 a는 마지막 main 커밋에 "x"를 포함할 것.
test "$(git show main:a)" = x
# 파일 b는 마지막 main 커밋에 "x"를 포함할 것.
test "$(git show main:b)" = x
# 파일 c는 마지막 main 커밋에 "x"를 포함할 것.
test "$(git show main:c)" = x