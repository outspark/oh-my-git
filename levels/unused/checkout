title = Getting the last version
cards = checkout-file

[description]

당신은 오랫동안 에세이를 작성해왔습니다. 하지만 - 아, 이런! 고양이가 키보드 위를 걸어 다니면서 "도와줘서" 이제 엉망이 되어 버렸어요! :/

하지만 Git이 도와줄 수 있습니다! 고양이가 변경한 모든 사항을 취소하고, 마지막 커밋의 버전으로 돌아가려면 `checkout`을 사용하세요!

[setup]

echo "A" >> essay.txt
git add .
git commit -m "Initial commit"

echo "B" >> essay.txt
git commit -a -m "Improved version"

echo "C" >> essay.txt
git commit -a -m "Even better version"

echo "D" >> essay.txt
git commit -a -m "Marvelous version"

echo "blarg
blaaaargh" > essay.txt

[win]

# 마지막 커밋의 버전으로 복원하세요.
cat essay.txt | grep D