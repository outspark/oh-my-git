title = Deleting and renaming a remote
cards = checkout

[description]

여기, 당신은 이미 두 개의 원격 저장소를 설정했습니다! `git remote`를 사용하여 목록을 확인할 수 있습니다.

[setup]

git remote rename friend frend

[setup friend]

[setup enemy]

[win]

# 오타가 있는 원격 저장소의 이름을 변경합니다 (`git remote rename [old name] [new name]` 사용)
git remote | grep friend
# 오타가 있는 원격 저장소가 사라졌습니다.
! grep 'frend' <(git remote)
# 유지하고 싶지 않은 원격 저장소를 삭제합니다 (`git remote remove [remote]` 사용)
! grep 'enemy' <(git remote)