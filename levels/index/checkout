title = Checking out files from the index
cards = add reset-file checkout-file commit

[description]

파일을 수정했는데 변경사항을 유지하고 싶지 않나요? `git checkout`을 사용하면 됩니다!

이미 인덱스를 업데이트한 파일 c처럼 된 경우엔 어떻게 해야 할까요? 먼저 인덱스를 초기화해야 합니다!

[setup]

echo a > a
echo b > b
echo c > c
git add .
git commit -m "Initial commit"
echo x > a
echo x > b
echo x > c
git add c

[win]

# 로컬 파일에서 모든 변경사항을 제거하세요!
test "$(git diff --name-only | wc -l)" -eq 0